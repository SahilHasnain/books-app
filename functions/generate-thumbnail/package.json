{
  "name": "generate-thumbnail",
  "version": "1.0.0",
  "description": "Appwrite function to generate PDF thumbnails",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "build": "echo 'No build required'",
    "format": "prettier --write ."
  },
  "dependencies": {
    "node-appwrite": "^21.1.0",
    "pdf-to-img": "^5.0.0"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
